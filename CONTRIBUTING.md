# Contributing

Changes are reviewed through pull requests. Run `terraform fmt -check -recursive`,
`terraform init -backend=false`, `terraform validate`, `terraform test`, `tflint`,
and `pre-commit run --all-files` before submitting a pull request.
